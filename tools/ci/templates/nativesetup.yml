# [Template] Compile MRTK inside Unity.

steps:
- task: PowerShell@2
  displayName: 'Build Native Dependencies'
  inputs:
    targetType: filePath
    filePath: .\tools\ci\scripts\buildNativeProjectPipeline.ps1
    arguments: >
      -ProjectPath: '${{ parameters.ProjectPath }}'